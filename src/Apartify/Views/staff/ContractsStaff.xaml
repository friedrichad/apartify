<Window x:Class="Apartify.Views.staff.ContractsStaff"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Payments" Height="500" Width="800" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

    <TextBlock Text="Contracts" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />

        <DataGrid x:Name="DataGridContracts" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ContractId" Binding="{Binding ContractId}" Width="80" />
                <DataGridTextColumn Header="Apartment" Binding="{Binding Apartment.Number}" />
                <DataGridTextColumn Header="Resident" Binding="{Binding Resident.FullName}" />
                <DataGridTextColumn Header="StartDate" Binding="{Binding StartDate}" />
                <DataGridTextColumn Header="EndDate" Binding="{Binding EndDate}" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="BtnBack" Content="Back" Width="80" Margin="0,0,8,0" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
                <Button x:Name="BtnRefresh" Content="Refresh" Width="80" Margin="0,0,8,0" />
                <Button x:Name="BtnCancel" Content="Delete Contract" Width="120" Margin="0,0,8,0" />
                <Button x:Name="BtnDone" Content="End Contract" Width="120" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>