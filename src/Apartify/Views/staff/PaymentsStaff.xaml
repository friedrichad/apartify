<Window x:Class="Apartify.Views.staff.PaymentsStaff"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Payments" Height="500" Width="800" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Service Fee Payments" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />

        <DataGrid x:Name="DataGridFees" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="FeeId" Binding="{Binding FeeId}" Width="60" />
                <DataGridTextColumn Header="Apartment" Binding="{Binding Apartment.Number}" />
                <DataGridTextColumn Header="Month" Binding="{Binding Month}" />
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" />
                <DataGridCheckBoxColumn Header="Paid" Binding="{Binding Paid}" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="BtnRefresh" Content="Refresh" Width="80" Margin="0,0,8,0" />
            <Button x:Name="BtnCancel" Content="Cancel Payment" Width="120" Margin="0,0,8,0" />
            <Button x:Name="BtnDone" Content="Mark Done" Width="120" />
        </StackPanel>
    </Grid>
</Window>