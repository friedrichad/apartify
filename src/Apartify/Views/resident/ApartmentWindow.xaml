<Window x:Class="Apartify.Views.resident.ApartmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Apartment Info"
        Height="320" Width="550">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView x:Name="lvApartment">
            <ListView.View>
                <GridView>

                    <GridViewColumn Header="Number"
                                    DisplayMemberBinding="{Binding Number}"
                                    Width="100"/>

                    <GridViewColumn Header="Floor"
                                    DisplayMemberBinding="{Binding Floor}"
                                    Width="80"/>

                    <GridViewColumn Header="Area (m²)"
                                    DisplayMemberBinding="{Binding Area}"
                                    Width="100"/>

                    <!-- 🔥 thêm Building -->
                    <GridViewColumn Header="Building"
                                    DisplayMemberBinding="{Binding Building}"
                                    Width="150"/>

                </GridView>
            </ListView.View>
        </ListView>

        <Button Grid.Row="1"
                Content="Close"
                Width="100"
                Margin="0,10,0,0"
                HorizontalAlignment="Right"
                Click="Close_Click"/>
    </Grid>
</Window>